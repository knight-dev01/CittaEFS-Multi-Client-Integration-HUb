services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: cittaefs
      POSTGRES_PASSWORD: localdev_password
      POSTGRES_DB: cittaefs_hub
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
